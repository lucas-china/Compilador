Class exemplo;
Int: numero;
Int fatorial (Int fat){
if (fat > 1){
return fat * fatorial(fat - 1);
} else {
return 1;
}
}
// comentárioVoid resultado (Int valor){

mensagem = Resultado;
print (mensagem, valor);
return;
}
Main{
print (FatorialdeNDigiteonumero”);
read (numero);
resultado (fatorial(numero));
}